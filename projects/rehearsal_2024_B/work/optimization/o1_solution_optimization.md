# Optimization Report — O1

## 1. Metadata

- Project: rehearsal_2024_B
- Stage: S2
- Current Gate: G2（待提交）
- Git Commit / Model Version: 6b88cd1eb6a4776b12b77b859f01d7b8cfaf9a39 / CONTRACT_ONLY_NO_MODEL_FIT
- Remaining Time: UNKNOWN；按下述保守时间盒约束后续工作
- Review basis: G1 Round 2 PASS，review commit fd6e33b4fa9a7f2de4764ff52272cd62ff52a45b
- Inputs reviewed: work/01_problem_analysis.md、work/02_data_audit.md、work/03_requirement_matrix.md、work/04_solution_plan.md、work/models/、work/05_experiment_plan.md、CURRENT.md、results/raw/s2/

## 2. Current State

- 已完成：统一 Q1→Q2/Q3 数据流、共享特征引擎、三问模型合同、实验计划、可机读配置、严格分组切分注册表与 Q3 合成指标测试。
- 当前模型与结果：尚未拟合任何预测模型；当前只有合同校验证据。正式校验为 PASS，模型拟合数 0，官方测试集数值读取数 0。
- 已批准成果：G1 Round 2 已批准 S1 的题意、A01–A06、严格身份、跨文件重复、测试封存及 Q3 两级目标/指标合同。
- 数据规模：A01 后训练候选 1,250 个 AP 行、482 个 source_file+test_id 组；Q2 固定 17 个联合标签；13 个 source_file 场景。
- 切分证据：3×5 外层分组折共 15 折、1,446 个外层组分配、5,784 个嵌套内层组分配、13 个 LOSO 分割已冻结。
- 当前边界：G2 PASS 前只允许合同与切分验证，不允许正式 Baseline 训练、调参或官方测试预测。

## 3. Evidence-Based Bottlenecks

| ID | 瓶颈 | 证据 | 状态 | 影响 |
|---|---|---|---|---|
| B1 | RSSI 为变长/缺失感知输入，且场景含 2 AP 与 3 AP | S1 审计确认 AP 数量分层和全空 RSSI 方向；work/02 已冻结 A02 | 已由确定性摘要、缺失指示和对称 peer 聚合控制 | 若直接展开 ID 或固定槽位，会产生结构泄漏和不可迁移特征 |
| B2 | Q2/Q3 依赖 Q1 预测，存在下游使用真值或全量拟合预测的泄漏风险 | work/03 与 G1 复审明确要求 OOF 链路 | 已由同一外层注册表与逐折 cross-fitting 设计控制 | 违反时会系统性高估 Q2/Q3 |
| B3 | Q2 联合标签不平衡，训练折可能缺少全局类别 | 正式校验固定 17 类并记录各折 support/缺类 | 已冻结零概率回填、固定标签 macro-F1 和 A03 敏感性 | 不固定时各折指标不可比，稀有类失败会被隐藏 |
| B4 | Q3 同时要求 AP/系统两级输出，题面有符号误差可产生反直觉精度 | G1 批准的 Q3 合同；合成测试覆盖方向、零分母、并列、最近秩和大于 100% 精度 | 已由系统严格求和、题面主指标与绝对误差辅助指标控制 | 独立系统头或错误取绝对值会违反题意 |
| B5 | 普通分组 K 折仍共享 source 场景，不能证明场景外推 | G1 Minor；13 个 source_file | 强制单独报告 13 折 LOSO | 主 CV 良好但 LOSO 失稳时不得宣称泛化 |
| B6 | 训练样本小，候选过多会造成选择偏差和时间浪费 | 1,250 行、482 组；单一主要建模成员 | 已限制为简单 Baseline、共享 HGB 家族和 4 点网格 | 算法堆叠会增加多重比较且难以解释 |

## 4. Candidate Improvements

| 候选 | 对应瓶颈 | 预期收益 | 验证方式 | 时间成本 | 风险 | 回退版本 |
|---|---|---|---|---|---|---|
| C1 共享机制特征 + Dummy/Ridge/Logistic/物理效率 Baseline | B1、B2、B4 | 建立可解释、低方差下限并验证数据流 | 同一 3×5 OOF、LOSO、特征族消融 | S3 内 3–4 小时 | 欠拟合非线性交互 | Dummy 基线 |
| C2 三问共享 HGB 主家族，Q3 比较直接与物理残差两种头 | B1、B3、B4、B6 | 在小样本上表达阈值和非线性交互，同时保持预算可控 | 每问固定 4 点网格；Q3 最多 8 个结构×参数组合；嵌套选择 | S4 总计不超过 8 小时 | 稀有类或 LOSO 方差较大 | C1 Baseline |
| C3 按 AP 数量分别训练模型 | B1、B5 | 可能减少 2/3 AP 机制混合 | 仅在共同模型分层残差显著时做一次有界对照 | 最多 2 小时 | 子样本更小、增加维护成本 | 共享模型 |
| C4 独立系统吞吐量头 | B4 | 可能直接优化系统误差 | AP/系统一致性检查 | 约 2 小时 | 与“系统=AP 预测之和”交付约束冲突 | 严格求和 |
| C5 图神经网络或深层网络 | B1、B6 | 理论上表达拓扑关系 | 需要额外嵌套验证 | 超过 8 小时 | 数据量不足、复现和解释成本高 | C2 HGB |

## 5. Selected Action

- 选择：C1 作为 S3 必做 Baseline，C2 作为 G2 通过后唯一主候选家族；C3 仅在预先定义的分层失败证据出现时允许一次有界对照。
- 理由：该组合完整回答三问，以同一机制特征和同一分组注册表闭合 Q1→Q2/Q3 链路；它覆盖线性、物理与非线性三类必要比较，但没有算法堆叠。
- Q1：以 DummyMedian/Ridge 建下限，HGB 预测 seq_time，并以 held-out 消融、分组置换和跨折方向稳定性回答发送机会影响。
- Q2：只预测 17 个已观察联合标签；缺类折按零概率回填；主指标为固定标签 macro-F1。
- Q3：先用题面 PHY rate 构造受限效率基线，再比较直接 HGB 与非负残差 HGB；系统输出始终为 AP 输出之和。
- 未选择 C4：破坏 AP/系统严格一致性。
- 未选择 C5：1,250 行不足以支持其额外自由度，且超过当前复现与时间预算。
- 暂不执行 C3：只有共享模型在预注册 AP-count 分层上出现稳定失效时才有证据启动。

## 6. Time Box

- 最大允许时间：G2 PASS 后 S3 Baseline 不超过 6 小时；S4 主候选总计不超过 8 小时；任何条件候选单次不超过 2 小时。
- 检查点 1（S3 1 小时）：特征、组键、OOF 路由和输入禁用断言全部 PASS。
- 检查点 2（S3 4 小时）：三问 Baseline、15 外层折与 13 LOSO 均有完整原始结果。
- 检查点 3（S3 6 小时）：报告、失败分析和 O2 输入齐备；否则停止扩展。
- 检查点 4（S4 4/8 小时）：只保留达到合同晋级阈值且不恶化 LOSO/偏差护栏的候选。

## 7. Success Criteria

1. 所有折的 source_file+test_id 训练/验证集合零重叠，所有可学习预处理仅在当前训练折拟合。
2. Q1/Q2/Q3 Baseline 均产生可复核的逐折 JSON/JSONL 结果；Q2 始终使用固定 17 类；Q3 AP/系统指标与严格求和恒等式通过。
3. 15 折主 CV 与 13 折 LOSO 均完整报告，不以主 CV 替代外推压力测试。
4. 主候选只有在各合同的预注册阈值、复杂度 tie-break 和失败护栏全部满足时才可晋级。
5. 原始输入哈希不变，官方测试数值在模型冻结前继续封存，所有中间结果不使用 CSV 格式。

## 8. Stop Conditions

1. 任一身份、分组、输入哈希、折间重叠、预处理作用域或 cross-fitting 断言失败。
2. 发现 Q1 使用事后字段、Q2 使用真实 Q1/Q2 目标，或 Q3 使用未授权真实结果字段。
3. 主候选未达到合同改善阈值，或改善只来自单一 repeat、单一场景或测试集反馈。
4. LOSO 明显失稳、Q3 有符号偏差护栏恶化，或系统求和一致性失败。
5. 达到候选数量/时间盒后停止，不临时扩大网格或新增算法。
6. G2 未 PASS 时立即停止在合同验证边界，不进入任何模型拟合。

## 9. Rollback Plan

若 C2 不满足晋级标准，回退到 C1 中表现最稳健的简单 Baseline，并保留同一特征、切分与评估注册表。若发现 S2 合同缺陷，停止 S3/S4，回到最后一个输入哈希不变且 split registry 校验 PASS 的提交 6b88cd1eb6a4776b12b77b859f01d7b8cfaf9a39，修订合同后重新提交 G2。任何失败运行写入实验日志，不覆盖最近一次有效原始结果。

## 10. Advisor Input（如有）

- 文件：无。
- 采纳：无；本轮没有调用战略顾问。
- 拒绝及理由：无。

## 11. Decision

- O1: PROCEED_TO_G2
- 依据：三问覆盖、逐问合同、可获得输入、分组/嵌套/LOSO 验证、测试封存、有限候选预算、停止条件与回退路径均已明确；正式合同校验 PASS 且未拟合模型。
- 授权边界：仅允许提交 G2。G2 Reviewer 给出 PASS 前不得进入 S3。
